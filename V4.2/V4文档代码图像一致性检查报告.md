# V4文档—模型—图像一致性检查报告

> 检查日期：2026-07-25  
> 活动基线：`V4整合/`  
> 公共Schema：`BLUE_HUB_CH4_SCHEMA_V2 / 2.0.0`

## 1. 检查范围

本次检查覆盖V4内43个Markdown文档、2个DOCX、1个PDF、1个XLSX，以及MATLAB/Python模型、
配置、CSV结果与所有Markdown图片引用。历史归档只通过索引定位4.3原图，未递归扫描。

一致性按三层区分：

1. **V4联合结果**：必须与`run_v4_integration`及`outputs/v4_hourly_summary.csv`一致；
2. **专业模块独立算例**：必须与对应脚本、参数和CSV一致，并明确不是V4联合结果；
3. **历史/方法快照**：必须在README中标明追溯用途，不得冒充当前活动结果。

## 2. 发现与修复

| 模块 | 发现 | 修复后状态 |
|---|---|---|
| 顶层说明 | 仍写全部价格均为假设值，并引用2026-07-20旧结果 | 已改为公开市场情景锚点与待校准参数混合口径；结果更新至2026-07-25 |
| 4.1—4.2 | 变量字典未列出4.5新增水耗、储氢损失、启动和FLEH | 已作为不改变冻结功率端口语义的可选诊断字段补入字典和配置 |
| 4.3 | 6幅图指向已迁移的`../输出/figures` | 已按历史索引SHA-256恢复到`docs/assets/`并改为相对路径 |
| 4.4 | `报告.pdf`仍是早期场景，包含旧PUE、旧功率和旧残差 | 已用当前V4小时结果重建PDF和母线平衡图 |
| 4.5 | 6幅图引用本机临时目录；脚本仍用100 MWh BESS和30 t初始库存 | 已改为`docs/assets/`；脚本统一为20 MW/40 MWh、24 t初始库存并重跑 |
| 4.5表格 | 弃电、外送、场景价值和残差仍是旧脚本结果 | 已由新生成CSV回填；最大独立算例残差为4.131×10^-13 MW |
| 4.6 | 文档与配置需核对PUE、电价和DC上限 | 配置与文档一致：PUE设计锚点1.076、401.34 CNY/MWh、8.20 MW |
| 4.8 | 旧文档将价格和成本全部称为假设值 | 已区分28.36 CNY/kg氢价、347 CNY/MWh年度电价情景锚点与其他待校准参数 |
| 4.9 | README未体现合同补产和边际价值比较 | 已与`v4_dispatch_4_9.m`当前规则同步 |

## 3. 图片和附件检查

- Markdown共13个图片引用，全部已解析为V4内存在的相对路径，无临时目录和失效引用；
- 4.3的6幅图为Version3合成数据图，来源文件哈希与历史索引一致；
- 4.4图直接读取当前`v4_hourly_summary.csv`，并已嵌入重建后的2页PDF；
- 4.5的6幅图由当前`blue_hub_demo_and_figures.m`重新生成，数据与同目录CSV一致；
- 4.5 DOCX含6幅内嵌图片，均为inline对象，不依赖外部文件；该DOCX与XLSX在README中明确为方法快照；
- XLSX共5个工作表，公式错误扫描为0，五个工作表均完成渲染检查。

DOCX自动渲染组件因本机缺少LibreOffice而不可用；Word后台导出也未在限定时间内完成。
因此DOCX只完成结构、内嵌图片和文本口径检查，未宣称通过页面级视觉渲染门槛。

## 4. 当前联合结果基准

2026-07-25重新运行V4联合入口得到：

| 指标 | 当前结果 |
|---|---:|
| 最大公共母线残差 | 4.591×10^-14 MW |
| 海洋未供能 | 48.1031 MWh |
| 4.8目标向量 | [-1,104,846.17 CNY, 33,275.27 kgCO2e, 48.1031 MWh] |
| 当前制氢量 | 0 kg |
| 当前工艺给水量 | 0 m³ |

当前制氢量为0是40 MW `interface_smoke`源侧、刚性负荷与当前可行规则共同作用的结果，
不代表100 MW工程候选或4.5独立方法算例不制氢。

## 5. 测试与适用边界

- 4.6 PUE设计锚点测试：通过；
- 4.6通过V4桥接器与MATLAB联合入口完成验证；系统Python环境未安装`h5py`，
  因而未把依赖原始海温HDF5数据的独立命令行入口列为本次通过项。桥接器在缺少该依赖时
  按文档采用23.5 °C回退值 **[假设值，待企业调研校准]**；
- 4.7原生单元测试：17/17通过；
- 4.5独立MILP图表脚本：通过；
- V4联合入口：通过；
- V4全量MATLAB验收：8/8通过；
- 便携包相对哈希校验：174/174通过。

本检查证明的是“当前文件、代码、图像和所标明的算例边界一致”，不把独立100 MW方法算例、
4.3合成100 MW源侧图或公开价格情景解释为V4 40 MW联合结果或工程承诺。未签认参数继续标记
**[假设值，待企业调研校准]**；未核验公式继续标记 **[需查证文献支撑]**。
