# C4 项目归档整理记录（2026-07-23）

## 整理结论

- `V4整合/` 已设为唯一活动成果，项目根目录不再并列放置 4.1—4.7 旧研发树；
- 4.1—4.7 旧研发资料与 4.8—4.9 架构原文已整体移入 `2026-07-23_V4前研发资料/01_模块研发源资料/`；
- 2026-07-19 的旧工具和修改前记录已统一归入 `2026-07-19_早期整理记录/`；
- 项目导航、工作区读取规则、历史批次索引、快速查找说明和文件级索引均已建立；
- V4 的运行代码、模块边界和公共接口未擅自改动。

## 去冗余结果

| 项目 | 处理 |
|---|---|
| 4.6 两份历史大 CSV | 原始合计约 223.4 MB，压缩为约 67.7 MB ZIP，并以原文件 SHA-256 逐条验证 |
| 4.6 `base_case/` 展开结果 | 与已有 `base_case.zip` 逐文件哈希一致，删除展开副本 |
| Python `__pycache__`、`.pyc` | 从 V4 和历史研发树中清除 |
| V4—历史相同内容 | 识别 111 个同哈希文件、约 2.82 MB；为维持旧模块结构和源快照重建能力，不物理删除，映射见 `V4与历史同哈希映射.csv` |
| 4.3 `node_modules` | 属于可再安装缓存；23.11 MB 本地二进制被当前运行进程锁定，已改名标识为非成果，待进程释放后删除 |

本轮实际占用由整理前约 262.9 MB 降至约 105.5 MB，减少约 157.4 MB。数值按文件长度统计，不包含空目录和文件系统簇开销。

## 可追溯性

- `历史文件索引.csv` 记录历史文件的相对路径、文件名、扩展名、大小、修改时间和 SHA-256；
- `V4与历史同哈希映射.csv` 将历史同内容文件映射到 V4 当前路径；
- `2026-07-23_V4前研发资料/03_大体量数据压缩包/4.6_large_historical_csv_sha256.csv` 记录压缩包内两份大 CSV 的原路径、条目名、字节数和 SHA-256；
- 所有新增导航、索引和构建说明均使用相对路径，不依赖本机绝对地址。

## 验证结果

- V4 历史源快照重建：79 个来源文件全部匹配；
- V4 可移植包校验：132 个包内相对路径文件全部匹配；
- MATLAB 全量验收：8/8 通过；
- 4.1—4.2 冻结契约、4.3 验收、4.3—4.9 联合链路、4.9 约束、4.8 三目标评价和 4.4 总账均通过；
- 最大母线功率平衡残差：`0.000e+00 MW`。

联调输出中的经济、排放和运行参数仍属于 `[假设值，待企业调研校准]`，本次整理不改变其证据等级。
