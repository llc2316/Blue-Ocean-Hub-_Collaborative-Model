# 2026-07-23 V4 前研发资料

## 状态

本批次是 V4 归档时点的历史源资料，不是当前交付版本。当前运行、修改和验收均应在项目根目录 `V4整合/` 中完成。

## 目录

- `01_模块研发源资料/`：保留原项目根目录下 4.1—4.7 研发树和 `08_4.8-4.9目标架构.md`，可作为 V4 源快照重建输入；
- `03_大体量数据压缩包/`：4.6 历史大 CSV 的压缩包和逐文件 SHA-256 清单。

## V4 源快照重建

仅在明确需要重新纳入旧源快照时，从 `V4整合/` 执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\build_v4_snapshot.ps1 `
  -SourceRoot "..\90_历史工作归档\2026-07-23_V4前研发资料\01_模块研发源资料"
```

普通运行、复制和打包检查只使用 `-VerifyOnly`，不读取本归档。

## 空间治理记录

- 两份大 CSV 原始合计约 223.4 MB，压缩后约 67.7 MB，压缩包已经逐条哈希复核；
- `base_case/` 展开结果与现存 `base_case.zip` 内容一致，已删除展开副本；
- `__pycache__` 和 `.pyc` 已清除；
- 4.3 的可再安装依赖缓存不作为成果，待占用进程释放后删除。
