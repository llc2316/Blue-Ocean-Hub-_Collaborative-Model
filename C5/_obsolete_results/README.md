# 作废结果隔离区

本目录不属于当前有效证据包。

`annual_pre_flexible_compute_and_effective_reserve/` 是在以下旧口径下生成的年度结果：

- 年度算力包含刚性基础负荷并计入 ENS；
- BESS 储备目标仅记录，未作为可回退的实际约束。

当前代码已经将比较场景中的算力统一定义为柔性负荷，并执行“产品份额约束释放 → BESS 储备约束释放 → 最小可行 ENS → 经济优化”的可靠性层级。因此旧结果不得用于正文、排名或结论，只为复核修改原因而保留。

状态：`OBSOLETE_DO_NOT_CITE`。

