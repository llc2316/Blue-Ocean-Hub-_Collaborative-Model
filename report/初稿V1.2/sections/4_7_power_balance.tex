\section{公共母线功率平衡}
\label{sec:power-balance}

各发电、储能和负荷模块在同一交流母线处进行功率核算。图~\ref{fig:bus-ledger}给出了
母线注入项、消耗项及残差检查关系。
\begin{figure}[H]
\centering
\resizebox{0.90\textwidth}{!}{\input{figures/chapter4/fig4-7_bus_ledger}}
\caption{公共母线功率平衡关系}
\label{fig:bus-ledger}
\sourcetype{本研究绘制。}
\end{figure}

\subsection{源侧功率分解}

公共汇集点的可用源功率分为实际利用功率和弃电功率：
\begin{equation}
P_t^{src,av}=P_t^{src,use}+P_t^{curt},
\qquad 0\le P_t^{src,use}\le P_t^{src,av}.
\label{eq:bus-source-split}
\end{equation}
$P_t^{curt}$仅表示已具备发电条件但未进入母线的功率。资源不足、环境停机和设备故障
造成的不可用功率在4.2中另行统计。

\subsection{逐时功率平衡}

在不允许外购电的离网边界下，逐时功率平衡为
\begin{equation}
\begin{aligned}
P_t^{src,use}+P_t^{dis}+P_t^{H2P}
={}&P_t^{ch}+P_t^{EL}+P_t^{aux,H2}+P_t^{DC}
+P_t^{cab,s}+P_t^{mar}\\
&+\left(P_t^{int,dem}-P_t^{ENS,int}\right)
+P_t^{aux,src}+P_t^{aux,com}+P_t^{loss,post}.
\end{aligned}
\label{eq:master-bus}
\end{equation}
若场景允许应急外购电，可在等式左侧增加$P_t^{imp}$，并同步计入购电成本和排放。

各类损耗只在所属模块中计算一次。$P_t^{DC}$已包含IT、冷却、固定辅助和供配电损耗；
电解系统若采用系统侧SEC，则常规整流和BOP用电不再重复计入；电池效率在状态方程中处理；
海缆损耗在送端与受端关系中处理。未供能和算力任务未服务量属于可靠性结果，不作为母线
注入项参与平衡。

\subsection{平衡残差}

定义母线残差为
\begin{equation}
r_t^{bus}=P_t^{in}-P_t^{out},
\qquad
\max_{t\in\mathcal T}|r_t^{bus}|\le\varepsilon^{bus}.
\label{eq:bus-residual}
\end{equation}
求解后同时检查电池能量递推、氢气质量守恒和产品结算量残差。$\varepsilon^{bus}$仅用于
浮点误差检查；若出现单位不一致、容量越界或非有限值，该时段结果不进入统计汇总。
